//code to check a given no is positive or not
#include<stdio.h>
#include<conio.h>
main()
{
int a;
printf("enter a no a");
scanf("%d",&a);
if(a>=0)
printf("the entered no is positive");
else
printf("the entered no is negative");
getch();
}
